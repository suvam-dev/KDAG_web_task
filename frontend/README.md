*To run Frontend:*
cd fronted && npm run dev  → This will run the frontend on http://localhost:5173/

* To run BackEnd:*
cd Backedn && node index.js → This will run the backend on http://localhost:3000/